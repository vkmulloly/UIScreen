/** @format */

import { AppRegistry } from 'react-native';
import ChatScreen from './App/Container/ChatScreen';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => ChatScreen);
